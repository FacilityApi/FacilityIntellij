service ExampleApi
{
    /// Identifies a widget field.
	[valid<caret>ate, obsolete]
    enum WidgetField
    {
        /// The 'id' field.
        id,

        /// The 'name' field.
        name,

        /// The 'weight' field.
        weight,
    }
}
